<h1>Alle brugere</h1>

<ul class="users">
  <% @users.each do |user| %>
      <li>
        <%= link_to user.email, user %>
        <% if current_user.admin? && !current_user?(user) %>
          | <%= link_to "slet", user, method: :delete,
                                      data: {confirm: "Er du sikker?" } %>
        <% end %>
      </li>
  <% end %>
</ul>